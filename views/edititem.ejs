<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit ToDo Item</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container py-5">
    <h2 class="mb-4 text-center">✏️ Edit ToDo Item</h2>

    <form action="/todo/edit/<%= todoIdx %>?_method=PUT" method="POST" class="card p-4 shadow-sm">
      <div class="mb-3">
        <label for="editTodo" class="form-label">Task</label>
        <input type="text" id="editTodo" name="editTodo" class="form-control" value="<%= todo %>" required>
      </div>
      <div class="d-flex justify-content-between">
        <a href="/todo" class="btn btn-secondary">Cancel</a>
        <button type="submit" class="btn btn-success">Save Changes</button>
      </div>
    </form>
  </div>

</body>
</html>
